# 要跑在 Linux 上的文件必须是 LF 行尾。
#
# 这台开发机是 Windows，git 默认 checkout 时把 LF 转成 CRLF。带 CRLF 的
# shell 脚本传到服务器上会报 `bash: $'\r': command not found` —— 报错信息
# 完全看不出是行尾问题，能查很久。systemd unit 和 nginx.conf 同理。
*.sh      text eol=lf
*.service text eol=lf
*.conf    text eol=lf
